import { Navbar } from 'react-bootstrap'

const navbarInstance = (
    <Navbar>
        <Navbar.Header>
            <Navbar.Brand>
                <a href="#">HP Predict</a>
            </Navbar.Brand>
        </Navbar.Header>
    </Navbar>
)

export default () => (navbarInstance)